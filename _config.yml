
title: Marges
description: joli blog
baseurl: "" # the subpath of your site, e.g. /blog
url: https://clairezed.github.io/marges/ # the base hostname & protocol for your site
# Build settings
markdown: kramdown

# source:
# destination:
# safe: true
# exclude: [Gemfile, Rakefile, readme.md]
include:
  - _pages
# keep_files: []
timezone: Europe/Paris
encoding: utf-8
# permalink: /:categories/:title/

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" #pages, posts, drafts
    values:
      layout: "default"

collections:
  studies:
    output: true
    permalink: /:collection/:path/
    title: Dossiers